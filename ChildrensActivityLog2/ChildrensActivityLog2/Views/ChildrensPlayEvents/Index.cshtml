@model IEnumerable<ChildrensActivityLog2.Models.ChildrensPlayEvents>

@{
    ViewData["Title"] = "Play-Event-Log";
}

<h2>Play-Event-Log</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Child)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.PlayEvent)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Child.FirstName)
                @Html.DisplayFor(modelItem => item.Child.LastName)
            </td>
           
            <td>
                @Html.DisplayFor(modelItem => item.PlayEvent.Title)
                @Html.DisplayFor(modelItem => item.PlayEvent.StartDate)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new { childId=item.ChildId, playEventId = item.PlayEventId  })
            </td>
        </tr>
}
    </tbody>
</table>
